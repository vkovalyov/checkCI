name: CI notify

on:
  workflow_run:
    workflows: ["CIbuild"]
    types: [completed]


jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: deepakputhraya/action-pr-title@master
        with:
          regex: 'ITRAPP-\d+'
          max_length: 50



